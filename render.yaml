# Render.com deployment configuration
services:
  - type: web
    name: fake-news-detector
    env: python
    buildCommand: pip install -r requirements.txt && python train_advanced_model.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
